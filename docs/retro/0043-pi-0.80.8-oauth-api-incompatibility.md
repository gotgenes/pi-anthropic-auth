---
issue: 43
issue_title: "Pi 0.80.8 removed `loginAnthropic`/`refreshAnthropicToken` from `@earendil-works/pi-ai/oauth`; /login fails with `(0 , _oauth.loginAnthropic) is not a function`"
---

# Retro: #43 — Pi 0.80.8 OAuth API incompatibility

## Stage: Planning (2026-07-17T00:00:00Z)

### Session summary

Transformed the pre-existing analysis doc `docs/plans/0043-*.md` into the formal implementation plan for restoring `/login` on Pi 0.80.8+.
Confirmed the fix direction (Option A — drop the `oauth` override, delegate login/refresh to Pi's built-in `anthropicOAuth`) with the operator via two `ask_user` rounds, the second after explaining the consequences of losing `mergeRefreshedCredentials`.
The plan is a removal + dependency-floor raise + docs pass, so it dispatches to `/build-plan`, not `/tdd-plan`.

### Observations

- **Option A vs B was a genuine `ask_user` gate.** Even though the operator authored the issue recommending Option A, the trade-off (dropping `mergeRefreshedCredentials`) is breaking and fallback-semantics-shaped, so I surfaced it. The operator chose Option A, accepting the refresh-rotation risk; Option B (vendoring the flow) and an upstream follow-up were both explicitly declined and recorded as Non-Goals.
- **Consequence of losing `mergeRefreshedCredentials`, verified from source:** built-in `refreshAnthropicToken` (0.80.8) sets `refresh: data.refresh_token` verbatim; an omitted field becomes `undefined` and breaks the next refresh until manual re-login. Only bites if Anthropic ever omits a rotated token (RFC 6749 §6 permits it; observed behavior always rotates).
- **Breaking-change classification:** the peer-floor raise to `>=0.80.8` follows Issue #40's precedent (floor raise released as a major). The plan puts `fix!:` + `BREAKING CHANGE:` on the floor-raise commit (step 2), not the removal commit (step 1).
- **Step ordering matters for typecheck:** the module deletion must land before/with the devDep bump. Bumping to `0.80.8` while `src/anthropic-oauth.ts` still imports the removed functions from the now-types-only `/oauth` subpath would fail `tsc`. Step 1 deletes on `0.80.2` (functions still present); step 2 bumps and re-validates against `0.80.10`.
- **CHANGELOG is release-please-generated** — no manual `[Unreleased]` edit; the Conventional Commit subjects/footers drive it. Corrected the earlier issue-body assumption that a manual CHANGELOG entry was needed.
- **Operator-confirmed doc path fix folded in:** `~/development/pi-mono` references in `AGENTS.md` are wrong; the real clone is `~/development/pi/pi`. Added to the docs step (step 3) since `AGENTS.md` is already being edited.
- **Latest pi-ai is `0.80.10`** — chosen as the devDep pin target.
- Historical plan/retro records that mention the override (`0031`, `0033`, `0037`, `gap-analysis-and-next-steps.md`, retro `0027`) are left as-is (Non-Goals) — point-in-time records.

## Stage: Implementation — Build (2026-07-17)

### Session summary

Implemented all three plan steps as four commits (the third being a pre-completion WARN fixup). Step 1 dropped the `oauth` override (`src/index.ts` edit + deletion of `src/anthropic-oauth.ts` and `test/anthropic-oauth.test.ts`); step 2 raised the peer floor to `>=0.80.8` and devDep pins to `0.80.10` via `fix!:` + `BREAKING CHANGE:` footer; step 3 updated `AGENTS.md`, `docs/architecture.md`, `.pi/skills/anthropic/SKILL.md` (also fixing stale `0.80.0` floor markers and `~/development/pi-mono` → `~/development/pi/pi`). Final state green: `pnpm run check`, `pnpm test` 49/49, `pnpm run lint`, `pnpm fallow:dead-code` all clean.

### Observations

- **Operator mid-build reminder on breaking-change framing:** the operator flagged mid-build that the floor raise is a breaking change; this was already the plan (`fix!:` + `BREAKING CHANGE:` on step 2 per Issue #40 precedent), and the reminder confirmed the commit verb was chosen correctly. lesson reinforced: surface the breaking form explicitly in the commit subject, not only the footer.
- **`pnpm-workspace.yaml` was touched by `pnpm install`** (added `minimumReleaseAgeExclude` entries for the four new pi packages); folded into the step-2 commit since it is part of the dependency bump. Not called out in the plan but harmless and necessary for a clean lockfile.
- **Stale `0.80.0` floor markers** in `AGENTS.md` (the Issue #28 clobber line and the "minimum supported host" gotcha) and the synced-skill paths would have been left behind if the docs step only targeted override references — folded the floor→`0.80.8` corrections into the docs commit, plus the operator-confirmed `~/development/pi-mono` path fix.
- **Pre-completion reviewer: WARN (non-blocking).** All deterministic checks PASS; conventional-commit hygiene PASS; forward/reverse doc review PASS except two notes: (1) `AGENTS.md` "Important upstream behavior" item 1 still described the `oauth` override capability without caveat — applied a one-line parenthetical `(still an available upstream capability, but this extension intentionally omits oauth since Issue #43 …)` as a follow-up `docs:` commit; (2) stale `pi-mono` paths in the **synced** `pi-extension-lifecycle` and `frontmatter` skills — explicitly out of scope (Non-Goals: synced skills corrected on the next `pi-packages` parity sync), no action taken.
- **No new tests** (the plan dispatches to `/build-plan`): the deleted behavior moves to Pi-owned code and the browser OAuth leg is not unit-testable here. Surviving suites (`oauth-transport`, `request-shaping`, `system-prompt-shaping`, `pi-anthropic-ordering-experiment`) pin all unchanged transport-wrapper invariants and stayed green.
- **Live `/login anthropic` smoke test against a real Pro/Max subscription on 0.80.8+ remains the only coverage for the delegated login path** and should be run before `/ship-issue` if feasible (browser leg cannot be automated here).
